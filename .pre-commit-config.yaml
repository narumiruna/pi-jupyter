repos:
  - repo: local
    hooks:
      - id: biome-check
        name: biome check
        entry: npm exec -- biome check --no-errors-on-unmatched
        language: system
        pass_filenames: true
        types_or: [javascript, jsx, ts, tsx, json]
